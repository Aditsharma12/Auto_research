# Environment / secrets — never commit these
.env
*.env

# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# HuggingFace cache (large model files)
# ~/.cache/huggingface  — leave this managed by HF itself

# Build artifacts
dist/
build/
*.egg-info/
